[package]
name = "rustynum-oracle"
version = "0.1.0"
edition = "2021"
description = "Three-temperature holographic oracle with capacity sweep for optimal parameter discovery"

[features]
default = []
avx512 = []
avx2 = []

[dependencies]
rustynum-core = { path = "../rustynum-core", default-features = false }
rand = "0.8"
serde_json = "1"

[dev-dependencies]
